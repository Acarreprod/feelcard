{%- render 'section-spacing-collapsing' -%}

<style>
  #shopify-section-{{ section.id }} {
    --product-grid: auto / minmax(0, 1fr);
    --product-gallery-media-list-grid: auto / auto-flow {% if section.settings.mobile_carousel_control == 'free_scroll' %}{% if section.settings.mobile_media_size == 'expanded' %}84vw{% else %}73vw{% endif %}{% else %}100%{% endif %};
    --product-gallery-media-list-gap: {% if section.settings.mobile_media_size == 'expanded' %}var(--spacing-0-5){% else %}var(--grid-gutter){% endif %};
  }

  @media screen and (max-width: 999px) {
    #shopify-section-{{ section.id }} {
      --section-spacing-block-start: {% if section.settings.mobile_media_size == 'expanded' %}0px{% else %}var(--container-gutter){% endif %};
    }
  }

  @media screen and (min-width: 1000px) {
    #shopify-section-{{ section.id }} {
      {%- assign media_ratio = section.settings.desktop_media_width | divided_by: 50.0 -%}
      --product-grid: auto / minmax(0, {{ media_ratio }}fr) minmax(0, {{ 2.0 | minus: media_ratio }}fr);
      --product-gallery-media-list-grid: {% if section.settings.desktop_media_layout contains 'grid' %}auto-flow dense / repeat({% if section.settings.desktop_media_layout == 'grid_one' %}1{% else %}2{% endif %}, minmax(0, 1fr)){% else %}auto / auto-flow 100%{% endif %};
      --product-gallery-media-list-gap: calc(var(--grid-gutter) / 2);
    }

    {%- if section.settings.desktop_media_layout == 'grid_highlight' -%}
    #shopify-section-{{ section.id }} .product-gallery__media-list > :not([hidden]) {
      grid-column: span 2;
    }
    #shopify-section-{{ section.id }} .product-gallery__media-list > :not([hidden]) ~ *:not(.product-gallery__media--expand) {
      grid-column: span 1;
    }
    {%- endif -%}
  }

  @media screen and (min-width: 1400px) {
    #shopify-section-{{ section.id }} {
      --product-gallery-media-list-gap: var(--grid-gutter);
    }
  }

  /* Styles exactement comme votre design */
  .product-offers-section {
    background: {{ section.settings.offers_bg | default: '#ffffff' }};
    padding: {{ section.settings.offers_padding | default: '32px 24px' }};
    margin: {{ section.settings.offers_margin | default: '32px 0' }};
    border-radius: {{ section.settings.offers_radius | default: 12 }}px;
    {% if section.settings.offers_border != blank %}
    border: {{ section.settings.offers_border }};
    {% endif %}
  }

  .offers-title-container {
    text-align: center;
    margin-bottom: 32px;
    position: relative;
  }

  .offers-title {
    font-size: {{ section.settings.title_size | default: 18 }}px;
    font-weight: {{ section.settings.title_weight | default: 700 }};
    color: {{ section.settings.title_color | default: '#000000' }};
    text-transform: {{ section.settings.title_transform | default: 'uppercase' }};
    letter-spacing: {{ section.settings.title_spacing | default: '1px' }};
    background: {{ section.settings.offers_bg | default: '#ffffff' }};
    padding: 0 20px;
    display: inline-block;
    position: relative;
    z-index: 2;
  }

  .offers-title-container::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 0;
    right: 0;
    height: 2px;
    background: {{ section.settings.title_line | default: 'linear-gradient(to right, #e0e7ff, #3b82f6, #e0e7ff)' }};
    z-index: 1;
  }

  .offer-option {
    position: relative;
    border: 2px solid {{ section.settings.option_border | default: '#e5e7eb' }};
    border-radius: {{ section.settings.option_radius | default: 16 }}px;
    padding: {{ section.settings.option_padding | default: '24px' }};
    margin-bottom: 16px;
    cursor: pointer;
    transition: all 0.3s ease;
    background: {{ section.settings.option_bg | default: '#ffffff' }};
  }

  .offer-option:hover {
    border-color: {{ section.settings.option_hover_border | default: '#d1d5db' }};
    box-shadow: {{ section.settings.option_hover_shadow | default: '0 4px 12px rgba(0, 0, 0, 0.08)' }};
    transform: {{ section.settings.option_hover_transform | default: 'translateY(-2px)' }};
  }

  .offer-option.selected {
    border-color: {{ section.settings.option_selected_border | default: '#3b82f6' }};
    background: {{ section.settings.option_selected_bg | default: '#f8faff' }};
    box-shadow: {{ section.settings.option_selected_shadow | default: '0 8px 25px rgba(59, 130, 246, 0.15)' }};
    transform: {{ section.settings.option_selected_transform | default: 'translateY(-4px)' }};
  }

  .offer-badge {
    position: absolute;
    top: -10px;
    right: 20px;
    padding: {{ section.settings.badge_padding | default: '8px 16px' }};
    border-radius: {{ section.settings.badge_radius | default: 20 }}px;
    font-size: {{ section.settings.badge_size | default: 12 }}px;
    font-weight: {{ section.settings.badge_weight | default: 600 }};
    color: {{ section.settings.badge_text_color | default: '#ffffff' }};
    z-index: 3;
    box-shadow: {{ section.settings.badge_shadow | default: '0 2px 8px rgba(0, 0, 0, 0.15)' }};
  }

  .offer-badge.popular {
    background: {{ section.settings.popular_color | default: '#10b981' }};
  }

  .offer-badge.best {
    background: {{ section.settings.best_color | default: '#f59e0b' }};
  }

  .offer-content {
    display: flex;
    align-items: flex-start;
    gap: 20px;
  }

  .offer-radio {
    width: 24px;
    height: 24px;
    border: 2px solid {{ section.settings.radio_border | default: '#d1d5db' }};
    border-radius: 50%;
    position: relative;
    flex-shrink: 0;
    margin-top: 2px;
    transition: all 0.3s ease;
  }

  .offer-option.selected .offer-radio {
    border-color: {{ section.settings.radio_selected_border | default: '#3b82f6' }};
    background: {{ section.settings.radio_selected_bg | default: '#3b82f6' }};
  }

  .offer-option.selected .offer-radio::after {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 8px;
    height: 8px;
    background: {{ section.settings.radio_dot_color | default: '#ffffff' }};
    border-radius: 50%;
  }

  .offer-details {
    flex: 1;
  }

  .offer-title {
    font-size: {{ section.settings.offer_title_size | default: 24 }}px;
    font-weight: {{ section.settings.offer_title_weight | default: 700 }};
    color: {{ section.settings.offer_title_color | default: '#000000' }};
    margin-bottom: 8px;
    line-height: 1.2;
  }

  .offer-bonus {
    font-size: {{ section.settings.bonus_size | default: 16 }}px;
    color: {{ section.settings.bonus_color | default: '#6b7280' }};
    margin-bottom: 4px;
    line-height: 1.3;
  }

  .offer-savings {
    font-size: {{ section.settings.savings_size | default: 16 }}px;
    color: {{ section.settings.savings_color | default: '#10b981' }};
    font-weight: {{ section.settings.savings_weight | default: 600 }};
  }

  .offer-pricing {
    text-align: right;
    flex-shrink: 0;
  }

  .offer-price {
    font-size: {{ section.settings.price_size | default: 28 }}px;
    font-weight: {{ section.settings.price_weight | default: 700 }};
    color: {{ section.settings.price_color | default: '#000000' }};
    line-height: 1;
    margin-bottom: 4px;
  }

  .offer-original-price {
    font-size: {{ section.settings.original_size | default: 16 }}px;
    color: {{ section.settings.original_color | default: '#9ca3af' }};
    text-decoration: line-through;
    font-weight: {{ section.settings.original_weight | default: 500 }};
  }

  .add-to-cart-section {
    margin: 32px 0;
  }

  .add-to-cart-btn {
    width: 100%;
    background: {{ section.settings.btn_bg | default: '#1a1a1a' }};
    color: {{ section.settings.btn_color | default: '#ffffff' }};
    border: none;
    padding: {{ section.settings.btn_padding | default: '16px 24px' }};
    border-radius: {{ section.settings.btn_radius | default: 8 }}px;
    font-size: {{ section.settings.btn_size | default: 18 }}px;
    font-weight: {{ section.settings.btn_weight | default: 700 }};
    text-transform: {{ section.settings.btn_transform | default: 'uppercase' }};
    letter-spacing: {{ section.settings.btn_spacing | default: '0.5px' }};
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .add-to-cart-btn:hover {
    background: {{ section.settings.btn_hover_bg | default: '#333333' }};
    transform: {{ section.settings.btn_hover_transform | default: 'translateY(-2px)' }};
    box-shadow: {{ section.settings.btn_hover_shadow | default: '0 8px 25px rgba(0, 0, 0, 0.2)' }};
  }

  .add-to-cart-btn:disabled {
    background: {{ section.settings.btn_disabled | default: '#9ca3af' }};
    cursor: not-allowed;
    transform: none;
    box-shadow: none;
  }

  .total-price {
    text-align: center;
    margin-top: 16px;
    font-size: {{ section.settings.total_size | default: 14 }}px;
    color: {{ section.settings.total_color | default: '#6b7280' }};
  }

  .total-price strong {
    color: {{ section.settings.total_strong_color | default: '#000000' }};
    font-weight: {{ section.settings.total_strong_weight | default: 700 }};
    font-size: {{ section.settings.total_strong_size | default: 18 }}px;
  }

  .stock-notice {
    background: {{ section.settings.stock_bg | default: 'linear-gradient(135deg, #fbbf24, #f59e0b)' }};
    color: {{ section.settings.stock_color | default: '#ffffff' }};
    padding: {{ section.settings.stock_padding | default: '12px 20px' }};
    border-radius: {{ section.settings.stock_radius | default: 8 }}px;
    text-align: center;
    font-weight: {{ section.settings.stock_weight | default: 600 }};
    margin-top: 16px;
    font-size: {{ section.settings.stock_size | default: 14 }}px;
  }

  .trust-badges {
    display: grid;
    grid-template-columns: repeat({{ section.settings.trust_cols | default: 3 }}, 1fr);
    gap: {{ section.settings.trust_gap | default: 16 }}px;
    margin-top: 32px;
    padding-top: 24px;
    border-top: 1px solid {{ section.settings.trust_border | default: '#e5e5e5' }};
  }

  .trust-badge {
    text-align: center;
    padding: {{ section.settings.trust_padding | default: '12px' }};
    border-radius: {{ section.settings.trust_radius | default: 8 }}px;
    transition: all 0.3s ease;
  }

  .trust-badge:hover {
    background: {{ section.settings.trust_hover_bg | default: '#f8f9fa' }};
    transform: {{ section.settings.trust_hover_transform | default: 'translateY(-2px)' }};
  }

  .trust-badge-icon {
    width: {{ section.settings.trust_icon_size | default: 24 }}px;
    height: {{ section.settings.trust_icon_size | default: 24 }}px;
    margin: 0 auto 8px;
    opacity: {{ section.settings.trust_icon_opacity | default: 0.7 }};
    color: {{ section.settings.trust_icon_color | default: '#6b7280' }};
  }

  .trust-badge-text {
    font-size: {{ section.settings.trust_text_size | default: 12 }}px;
    color: {{ section.settings.trust_text_color | default: '#6b7280' }};
    font-weight: {{ section.settings.trust_text_weight | default: 500 }};
  }

  .product-rating {
    display: flex;
    align-items: center;
    gap: 8px;
    margin: 16px 0;
  }

  .stars {
    display: flex;
    gap: 2px;
  }

  .star {
    width: {{ section.settings.star_size | default: 16 }}px;
    height: {{ section.settings.star_size | default: 16 }}px;
    fill: {{ section.settings.star_color | default: '#fbbf24' }};
  }

  .rating-text {
    font-size: {{ section.settings.rating_text_size | default: 14 }}px;
    color: {{ section.settings.rating_text_color | default: '#6b7280' }};
  }

  .professional-badge {
    display: inline-block;
    background: {{ section.settings.pro_badge_bg | default: '#f3f4f6' }};
    color: {{ section.settings.pro_badge_color | default: '#374151' }};
    padding: {{ section.settings.pro_badge_padding | default: '4px 12px' }};
    border-radius: {{ section.settings.pro_badge_radius | default: 20 }}px;
    font-size: {{ section.settings.pro_badge_size | default: 12 }}px;
    font-weight: {{ section.settings.pro_badge_weight | default: 600 }};
    margin-bottom: 16px;
  }

  .product-info__title {
    font-size: {{ section.settings.product_title_size | default: 32 }}px;
    font-weight: {{ section.settings.product_title_weight | default: 700 }};
    color: {{ section.settings.product_title_color | default: '#1a1a1a' }};
    margin-bottom: 16px;
    line-height: 1.2;
  }

  .product-info__description {
    font-size: {{ section.settings.description_size | default: 16 }}px;
    color: {{ section.settings.description_color | default: '#6b7280' }};
    line-height: 1.6;
    margin-bottom: 24px;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .offer-content {
      flex-direction: column;
      align-items: flex-start;
      gap: 16px;
    }

    .offer-pricing {
      text-align: left;
      width: 100%;
    }

    .trust-badges {
      grid-template-columns: 1fr;
      gap: 12px;
    }

    .offers-title {
      font-size: {{ section.settings.mobile_title_size | default: 16 }}px;
    }

    .offer-title {
      font-size: {{ section.settings.mobile_offer_title_size | default: 20 }}px;
    }

    .offer-price {
      font-size: {{ section.settings.mobile_price_size | default: 24 }}px;
    }

    .product-info__title {
      font-size: {{ section.settings.mobile_product_title_size | default: 24 }}px;
    }

    .product-offers-section {
      padding: {{ section.settings.mobile_offers_padding | default: '24px 16px' }};
      margin: {{ section.settings.mobile_offers_margin | default: '24px 0' }};
    }
  }
</style>

{%- capture product_form_id -%}product-form-{{ product.id }}-{{ section.id }}{%- endcapture -%}

<div {% render 'section-properties', tight: section.settings.full_width %}>
  <product-rerender id="product-info-{{ product.id }}-{{ section.id }}" observe-form="{{ product_form_id }}" allow-partial-rerender>
    <div class="product">
      {%- if product.media.size > 0 -%}
        {%- render 'product-gallery', product: product, product_form_id: product_form_id -%}
      {%- endif -%}

      <div class="product-info">
        {%- if section.settings.show_pro_badge -%}
          <span class="professional-badge">{{ section.settings.pro_badge_text | default: 'Professionnel' }}</span>
        {%- endif -%}

        <h1 class="product-info__title">{{ product.title }}</h1>

        {%- if section.settings.show_rating -%}
          <div class="product-rating">
            <div class="stars">
              {%- for i in (1..5) -%}
                <svg class="star" viewBox="0 0 24 24">
                  <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
                </svg>
              {%- endfor -%}
            </div>
            <span class="rating-text">({{ section.settings.reviews_count | default: 247 }} avis)</span>
          </div>
        {%- endif -%}

        {%- if section.settings.show_description and product.description != blank -%}
          <div class="product-info__description">
            <div class="prose">{{ product.description }}</div>
          </div>
        {%- elsif section.settings.custom_description != blank -%}
          <div class="product-info__description">
            <div class="prose">{{ section.settings.custom_description }}</div>
          </div>
        {%- endif -%}

        <div class="product-offers-section">
          <div class="offers-title-container">
            <h2 class="offers-title">{{ section.settings.offers_title | default: 'Choisissez votre offre :' }}</h2>
          </div>

          <form id="{{ product_form_id }}" action="/cart/add" method="post" enctype="multipart/form-data">
            <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}" id="variant-id-{{ section.id }}">
            
            <div class="offer-options">
              {%- assign sorted_variants = product.variants | sort: 'price' -%}
              {%- for variant in sorted_variants -%}
                {%- if variant.available -%}
                  {%- assign variant_title_lower = variant.title | downcase -%}
                  {%- assign quantity = 1 -%}
                  {%- if variant_title_lower contains '2' -%}
                    {%- assign quantity = 2 -%}
                  {%- elsif variant_title_lower contains '5' -%}
                    {%- assign quantity = 5 -%}
                  {%- elsif variant_title_lower contains '10' -%}
                    {%- assign quantity = 10 -%}
                  {%- endif -%}

                  {%- assign base_price = sorted_variants.first.price -%}
                  {%- assign expected_price = base_price | times: quantity -%}
                  {%- assign savings_amount = expected_price | minus: variant.price -%}
                  {%- assign savings_percent = 0 -%}
                  {%- if expected_price > 0 and savings_amount > 0 -%}
                    {%- assign savings_percent = savings_amount | times: 100 | divided_by: expected_price -%}
                  {%- endif -%}

                  {%- assign badge_text = '' -%}
                  {%- assign badge_style = 'popular' -%}
                  {%- assign bonus_text = '' -%}
                  {%- assign is_default = false -%}

                  {%- if quantity == 2 -%}
                    {%- assign badge_text = section.settings.badge_2_text | default: 'Le plus choisi' -%}
                    {%- assign badge_style = 'popular' -%}
                    {%- assign bonus_text = section.settings.bonus_2_text | default: '+ Ebook Gratuit' -%}
                  {%- elsif quantity == 5 -%}
                    {%- assign bonus_text = section.settings.bonus_5_text | default: '+ Ebook Gratuit & Cadeau 🎁' -%}
                    {%- if section.settings.default_selection == '5' -%}
                      {%- assign is_default = true -%}
                    {%- endif -%}
                  {%- elsif quantity == 10 -%}
                    {%- assign badge_text = section.settings.badge_10_text | default: 'Meilleure offre' -%}
                    {%- assign badge_style = 'best' -%}
                    {%- assign bonus_text = section.settings.bonus_10_text | default: '+ Ebook Gratuit & Cadeau 🎁' -%}
                  {%- endif -%}

                  {%- if forloop.first and is_default == false -%}
                    {%- assign is_default = true -%}
                  {%- endif -%}

                  <div class="offer-option{% if is_default %} selected{% endif %}" 
                       data-variant-id="{{ variant.id }}"
                       data-offer="{{ quantity }}" 
                       data-price="{{ variant.price }}" 
                       data-original="{{ expected_price }}"
                       data-savings="{{ savings_percent }}">
                    {%- if badge_text != blank -%}
                      <div class="offer-badge {{ badge_style }}">{{ badge_text }}</div>
                    {%- endif -%}
                    <div class="offer-content">
                      <div class="offer-radio"></div>
                      <div class="offer-details">
                        <div class="offer-title">{{ variant.title }}</div>
                        {%- if bonus_text != blank -%}
                          <div class="offer-bonus">{{ bonus_text }}</div>
                        {%- endif -%}
                        {%- if savings_percent > 0 -%}
                          <div class="offer-savings">Économisez {{ savings_percent }}%</div>
                        {%- endif -%}
                      </div>
                      <div class="offer-pricing">
                        <div class="offer-price">{{ variant.price | money_without_currency | replace: ',', '.' }} {{ section.settings.currency | default: '€ HT' }}</div>
                        {%- if savings_percent > 0 -%}
                          <div class="offer-original-price">{{ expected_price | money_without_currency | replace: ',', '.' }} {{ section.settings.currency | default: '€ HT' }}</div>
                        {%- endif -%}
                      </div>
                    </div>
                  </div>
                {%- endif -%}
              {%- endfor -%}
            </div>

            <input type="hidden" name="properties[Quantité]" id="selected-quantity-{{ section.id }}" value="">
            <input type="hidden" name="properties[Prix unitaire]" id="selected-unit-price-{{ section.id }}" value="">

            <div class="add-to-cart-section">
              <button type="submit" class="add-to-cart-btn" id="add-to-cart-button-{{ section.id }}">
                {{ section.settings.btn_text | default: 'AJOUTER AU PANIER' }}
              </button>
              <div class="total-price">
                Prix total: <strong id="total-price-display-{{ section.id }}">{{ product.selected_or_first_available_variant.price | money_without_currency | replace: ',', '.' }} {{ section.settings.currency | default: '€ HT' }}</strong>
              </div>
              {%- if section.settings.show_stock_notice -%}
                <div class="stock-notice">
                  {{ section.settings.stock_notice_text | default: "🔥 Stock Limité – VENTE D'ÉTÉ 🔥" }}
                </div>
              {%- endif -%}
            </div>
          </form>
        </div>

        {%- if section.settings.show_trust_badges -%}
          <div class="trust-badges">
            <div class="trust-badge">
              <svg class="trust-badge-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="m7 11 2-2-2-2"/>
                <path d="M11 13h4"/>
                <rect width="18" height="18" x="3" y="3" rx="2" ry="2"/>
              </svg>
              <div class="trust-badge-text">{{ section.settings.trust_badge_1 | default: 'Livraison rapide' }}</div>
            </div>
            <div class="trust-badge">
              <svg class="trust-badge-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
              </svg>
              <div class="trust-badge-text">{{ section.settings.trust_badge_2 | default: 'Garantie qualité' }}</div>
            </div>
            <div class="trust-badge">
              <svg class="trust-badge-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M3 7v10a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V9a2 2 0 0 0-2-2H5a2 2 0 0 0-2 2z"/>
                <path d="M8 7V5a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/>
              </svg>
              <div class="trust-badge-text">{{ section.settings.trust_badge_3 | default: 'Retour gratuit' }}</div>
            </div>
          </div>
        {%- endif -%}
      </div>
    </div>
  </product-rerender>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const sectionId = '{{ section.id }}';
  const offerOptions = document.querySelectorAll('#shopify-section-' + sectionId + ' .offer-option');
  const totalPriceDisplay = document.getElementById('total-price-display-' + sectionId);
  const selectedQuantityInput = document.getElementById('selected-quantity-' + sectionId);
  const selectedUnitPriceInput = document.getElementById('selected-unit-price-' + sectionId);
  const variantIdInput = document.getElementById('variant-id-' + sectionId);
  const addToCartButton = document.getElementById('add-to-cart-button-' + sectionId);
  
  let selectedOffer = document.querySelector('#shopify-section-' + sectionId + ' .offer-option.selected');
  if (!selectedOffer && offerOptions.length > 0) {
    selectedOffer = offerOptions[0];
    selectedOffer.classList.add('selected');
  }
  
  function updateSelection(option) {
    // Remove selected class from all options
    offerOptions.forEach(opt => opt.classList.remove('selected'));
    
    // Add selected class to clicked option
    option.classList.add('selected');
    selectedOffer = option;
    
    // Update variant ID
    if (variantIdInput) {
      variantIdInput.value = option.dataset.variantId;
    }
    
    // Update hidden inputs
    updateCartProperties();
    
    // Update total price
    updateTotalPrice();
    
    // Animation feedback
    option.style.transform = 'scale(0.98)';
    setTimeout(() => {
      option.style.transform = '';
    }, 150);
    
    // Update URL for variant (optional)
    if (window.history && window.history.replaceState) {
      const url = new URL(window.location);
      url.searchParams.set('variant', option.dataset.variantId);
      window.history.replaceState({}, '', url);
    }
  }
  
  offerOptions.forEach(option => {
    option.addEventListener('click', function(e) {
      e.preventDefault();
      updateSelection(this);
    });
  });

  function updateCartProperties() {
    if (selectedOffer && selectedQuantityInput && selectedUnitPriceInput) {
      const quantity = selectedOffer.dataset.offer;
      const price = parseFloat(selectedOffer.dataset.price) / 100;
      
      selectedQuantityInput.value = quantity;
      selectedUnitPriceInput.value = price.toFixed(2) + ' {{ section.settings.currency | default: "€ HT" }}';
    }
  }

  function updateTotalPrice() {
    if (selectedOffer && totalPriceDisplay) {
      const price = parseFloat(selectedOffer.dataset.price);
      const total = price / 100;
      totalPriceDisplay.textContent = total.toFixed(2).replace('.', ',') + ' {{ section.settings.currency | default: "€ HT" }}';
    }
  }

  // Form submission handling
  if (addToCartButton) {
    addToCartButton.addEventListener('click', function(e) {
      e.preventDefault();
      
      // Disable button during submission
      this.disabled = true;
      const originalText = this.textContent;
      this.textContent = '{{ section.settings.loading_text | default: "Ajout en cours..." }}';
      
      // Update cart properties before submission
      updateCartProperties();
      
      // Submit the form
      const form = document.getElementById('{{ product_form_id }}');
      if (form) {
        const formData = new FormData(form);
        
        fetch('/cart/add.js', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify({
            id: selectedOffer.dataset.variantId,
            quantity: 1,
            properties: {
              'Quantité': selectedOffer.dataset.offer,
              'Prix unitaire': (parseFloat(selectedOffer.dataset.price) / 100).toFixed(2) + ' {{ section.settings.currency | default: "€ HT" }}'
            }
          })
        })
        .then(response => response.json())
        .then(data => {
          this.textContent = '{{ section.settings.success_text | default: "Ajouté !" }}';
          setTimeout(() => {
            if ({{ section.settings.redirect_to_cart | default: true }}) {
              window.location.href = '/cart';
            } else {
              this.disabled = false;
              this.textContent = originalText;
            }
          }, {{ section.settings.success_delay | default: 1500 }});
        })
        .catch(error => {
          console.error('Error:', error);
          this.disabled = false;
          this.textContent = originalText;
          alert('{{ section.settings.error_text | default: "Erreur lors de l\'ajout au panier. Veuillez réessayer." }}');
        });
      }
    });
  }

  // Initialize
  if (selectedOffer) {
    if (variantIdInput) {
      variantIdInput.value = selectedOffer.dataset.variantId;
    }
    updateCartProperties();
    updateTotalPrice();
  }

  // Handle variant changes from URL
  const urlParams = new URLSearchParams(window.location.search);
  const variantFromUrl = urlParams.get('variant');
  if (variantFromUrl) {
    const matchingOption = Array.from(offerOptions).find(option => 
      option.dataset.variantId === variantFromUrl
    );
    if (matchingOption) {
      updateSelection(matchingOption);
    }
  }
});
</script>

{% schema %}
{
  "name": "Page Produit Offres",
  "class": "shopify-section--product-offers",
  "tag": "section",
  "settings": [
    {
      "type": "header",
      "content": "🎯 Configuration générale"
    },
    {
      "type": "checkbox",
      "id": "full_width",
      "label": "Pleine largeur",
      "default": true
    },
    {
      "type": "header",
      "content": "📝 Badge professionnel"
    },
    {
      "type": "checkbox",
      "id": "show_pro_badge",
      "label": "Afficher le badge professionnel",
      "default": true
    },
    {
      "type": "text",
      "id": "pro_badge_text",
      "label": "Texte du badge",
      "default": "Professionnel"
    },
    {
      "type": "color",
      "id": "pro_badge_bg",
      "label": "Arrière-plan",
      "default": "#f3f4f6"
    },
    {
      "type": "color",
      "id": "pro_badge_color",
      "label": "Couleur du texte",
      "default": "#374151"
    },
    {
      "type": "text",
      "id": "pro_badge_padding",
      "label": "Espacement",
      "default": "4px 12px"
    },
    {
      "type": "range",
      "id": "pro_badge_radius",
      "min": 0,
      "max": 50,
      "step": 2,
      "unit": "px",
      "label": "Bordure arrondie",
      "default": 20
    },
    {
      "type": "range",
      "id": "pro_badge_size",
      "min": 10,
      "max": 18,
      "step": 1,
      "unit": "px",
      "label": "Taille de police",
      "default": 12
    },
    {
      "type": "range",
      "id": "pro_badge_weight",
      "min": 400,
      "max": 900,
      "step": 100,
      "label": "Poids de police",
      "default": 600
    },
    {
      "type": "header",
      "content": "📖 Titre du produit"
    },
    {
      "type": "range",
      "id": "product_title_size",
      "min": 18,
      "max": 48,
      "step": 2,
      "unit": "px",
      "label": "Taille du titre",
      "default": 32
    },
    {
      "type": "range",
      "id": "mobile_product_title_size",
      "min": 16,
      "max": 32,
      "step": 2,
      "unit": "px",
      "label": "Taille mobile",
      "default": 24
    },
    {
      "type": "color",
      "id": "product_title_color",
      "label": "Couleur",
      "default": "#1a1a1a"
    },
    {
      "type": "range",
      "id": "product_title_weight",
      "min": 400,
      "max": 900,
      "step": 100,
      "label": "Poids",
      "default": 700
    },
    {
      "type": "header",
      "content": "⭐ Évaluations"
    },
    {
      "type": "checkbox",
      "id": "show_rating",
      "label": "Afficher les évaluations",
      "default": true
    },
    {
      "type": "number",
      "id": "reviews_count",
      "label": "Nombre d'avis",
      "default": 247
    },
    {
      "type": "range",
      "id": "star_size",
      "min": 12,
      "max": 24,
      "step": 2,
      "unit": "px",
      "label": "Taille des étoiles",
      "default": 16
    },
    {
      "type": "color",
      "id": "star_color",
      "label": "Couleur des étoiles",
      "default": "#fbbf24"
    },
    {
      "type": "range",
      "id": "rating_text_size",
      "min": 10,
      "max": 18,
      "step": 1,
      "unit": "px",
      "label": "Taille du texte",
      "default": 14
    },
    {
      "type": "color",
      "id": "rating_text_color",
      "label": "Couleur du texte",
      "default": "#6b7280"
    },
    {
      "type": "header",
      "content": "📄 Description"
    },
    {
      "type": "checkbox",
      "id": "show_description",
      "label": "Afficher la description du produit",
      "default": true
    },
    {
      "type": "textarea",
      "id": "custom_description",
      "label": "Description personnalisée",
      "info": "Utilisée si le produit n'a pas de description"
    },
    {
      "type": "range",
      "id": "description_size",
      "min": 12,
      "max": 20,
      "step": 1,
      "unit": "px",
      "label": "Taille de police",
      "default": 16
    },
    {
      "type": "color",
      "id": "description_color",
      "label": "Couleur",
      "default": "#6b7280"
    },
    {
      "type": "header",
      "content": "🎯 Section des offres"
    },
    {
      "type": "text",
      "id": "offers_title",
      "label": "Titre",
      "default": "Choisissez votre offre :"
    },
    {
      "type": "color",
      "id": "offers_bg",
      "label": "Arrière-plan",
      "default": "#ffffff"
    },
    {
      "type": "text",
      "id": "offers_padding",
      "label": "Espacement",
      "default": "32px 24px"
    },
    {
      "type": "text",
      "id": "mobile_offers_padding",
      "label": "Espacement mobile",
      "default": "24px 16px"
    },
    {
      "type": "text",
      "id": "offers_margin",
      "label": "Marge",
      "default": "32px 0"
    },
    {
      "type": "text",
      "id": "mobile_offers_margin",
      "label": "Marge mobile",
      "default": "24px 0"
    },
    {
      "type": "text",
      "id": "offers_border",
      "label": "Bordure",
      "info": "Ex: 1px solid #e5e7eb"
    },
    {
      "type": "range",
      "id": "offers_radius",
      "min": 0,
      "max": 24,
      "step": 2,
      "unit": "px",
      "label": "Bordure arrondie",
      "default": 12
    },
    {
      "type": "range",
      "id": "title_size",
      "min": 14,
      "max": 28,
      "step": 2,
      "unit": "px",
      "label": "Taille du titre",
      "default": 18
    },
    {
      "type": "range",
      "id": "mobile_title_size",
      "min": 12,
      "max": 20,
      "step": 1,
      "unit": "px",
      "label": "Taille mobile",
      "default": 16
    },
    {
      "type": "color",
      "id": "title_color",
      "label": "Couleur du titre",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "title_weight",
      "min": 400,
      "max": 900,
      "step": 100,
      "label": "Poids du titre",
      "default": 700
    },
    {
      "type": "select",
      "id": "title_transform",
      "label": "Transformation",
      "options": [
        {"value": "none", "label": "Aucune"},
        {"value": "uppercase", "label": "Majuscules"},
        {"value": "lowercase", "label": "Minuscules"},
        {"value": "capitalize", "label": "Première lettre"}
      ],
      "default": "uppercase"
    },
    {
      "type": "text",
      "id": "title_spacing",
      "label": "Espacement des lettres",
      "default": "1px"
    },
    {
      "type": "text",
      "id": "title_line",
      "label": "Dégradé de la ligne",
      "default": "linear-gradient(to right, #e0e7ff, #3b82f6, #e0e7ff)"
    },
    {
      "type": "header",
      "content": "💎 Options d'offres"
    },
    {
      "type": "select",
      "id": "default_selection",
      "label": "Sélection par défaut",
      "options": [
        {"value": "1", "label": "1 plaque"},
        {"value": "2", "label": "2 plaques"},
        {"value": "5", "label": "5 plaques"},
        {"value": "10", "label": "10 plaques"}
      ],
      "default": "5"
    },
    {
      "type": "color",
      "id": "option_bg",
      "label": "Arrière-plan",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "option_border",
      "label": "Couleur de bordure",
      "default": "#e5e7eb"
    },
    {
      "type": "color",
      "id": "option_hover_border",
      "label": "Bordure au survol",
      "default": "#d1d5db"
    },
    {
      "type": "color",
      "id": "option_selected_border",
      "label": "Bordure sélectionnée",
      "default": "#3b82f6"
    },
    {
      "type": "color",
      "id": "option_selected_bg",
      "label": "Arrière-plan sélectionné",
      "default": "#f8faff"
    },
    {
      "type": "range",
      "id": "option_radius",
      "min": 0,
      "max": 24,
      "step": 2,
      "unit": "px",
      "label": "Bordure arrondie",
      "default": 16
    },
    {
      "type": "text",
      "id": "option_padding",
      "label": "Espacement",
      "default": "24px"
    },
    {
      "type": "text",
      "id": "option_hover_shadow",
      "label": "Ombre au survol",
      "default": "0 4px 12px rgba(0, 0, 0, 0.08)"
    },
    {
      "type": "text",
      "id": "option_selected_shadow",
      "label": "Ombre sélectionnée",
      "default": "0 8px 25px rgba(59, 130, 246, 0.15)"
    },
    {
      "type": "text",
      "id": "option_hover_transform",
      "label": "Animation au survol",
      "default": "translateY(-2px)"
    },
    {
      "type": "text",
      "id": "option_selected_transform",
      "label": "Animation sélectionnée",
      "default": "translateY(-4px)"
    },
    {
      "type": "header",
      "content": "🎖️ Badges"
    },
    {
      "type": "text",
      "id": "badge_2_text",
      "label": "Badge 2 plaques",
      "default": "Le plus choisi"
    },
    {
      "type": "text",
      "id": "badge_10_text",
      "label": "Badge 10 plaques",
      "default": "Meilleure offre"
    },
    {
      "type": "color",
      "id": "popular_color",
      "label": "Couleur badge populaire",
      "default": "#10b981"
    },
    {
      "type": "color",
      "id": "best_color",
      "label": "Couleur meilleure offre",
      "default": "#f59e0b"
    },
    {
      "type": "text",
      "id": "badge_padding",
      "label": "Espacement",
      "default": "8px 16px"
    },
    {
      "type": "range",
      "id": "badge_radius",
      "min": 0,
      "max": 30,
      "step": 2,
      "unit": "px",
      "label": "Bordure arrondie",
      "default": 20
    },
    {
      "type": "range",
      "id": "badge_size",
      "min": 10,
      "max": 16,
      "step": 1,
      "unit": "px",
      "label": "Taille de police",
      "default": 12
    },
    {
      "type": "range",
      "id": "badge_weight",
      "min": 400,
      "max": 900,
      "step": 100,
      "label": "Poids de police",
      "default": 600
    },
    {
      "type": "color",
      "id": "badge_text_color",
      "label": "Couleur du texte",
      "default": "#ffffff"
    },
    {
      "type": "text",
      "id": "badge_shadow",
      "label": "Ombre",
      "default": "0 2px 8px rgba(0, 0, 0, 0.15)"
    },
    {
      "type": "header",
      "content": "🎁 Bonus"
    },
    {
      "type": "text",
      "id": "bonus_2_text",
      "label": "Bonus 2 plaques",
      "default": "+ Ebook Gratuit"
    },
    {
      "type": "text",
      "id": "bonus_5_text",
      "label": "Bonus 5 plaques",
      "default": "+ Ebook Gratuit & Cadeau 🎁"
    },
    {
      "type": "text",
      "id": "bonus_10_text",
      "label": "Bonus 10 plaques",
      "default": "+ Ebook Gratuit & Cadeau 🎁"
    },
    {
      "type": "range",
      "id": "bonus_size",
      "min": 12,
      "max": 20,
      "step": 1,
      "unit": "px",
      "label": "Taille de police",
      "default": 16
    },
    {
      "type": "color",
      "id": "bonus_color",
      "label": "Couleur",
      "default": "#6b7280"
    },
    {
      "type": "header",
      "content": "💰 Prix et économies"
    },
    {
      "type": "text",
      "id": "currency",
      "label": "Devise",
      "default": "€ HT"
    },
    {
      "type": "range",
      "id": "offer_title_size",
      "min": 16,
      "max": 32,
      "step": 2,
      "unit": "px",
      "label": "Taille titre offre",
      "default": 24
    },
    {
      "type": "range",
      "id": "mobile_offer_title_size",
      "min": 14,
      "max": 24,
      "step": 2,
      "unit": "px",
      "label": "Taille mobile",
      "default": 20
    },
    {
      "type": "color",
      "id": "offer_title_color",
      "label": "Couleur titre",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "offer_title_weight",
      "min": 400,
      "max": 900,
      "step": 100,
      "label": "Poids titre",
      "default": 700
    },
    {
      "type": "range",
      "id": "price_size",
      "min": 20,
      "max": 36,
      "step": 2,
      "unit": "px",
      "label": "Taille prix",
      "default": 28
    },
    {
      "type": "range",
      "id": "mobile_price_size",
      "min": 18,
      "max": 28,
      "step": 2,
      "unit": "px",
      "label": "Taille mobile",
      "default": 24
    },
    {
      "type": "color",
      "id": "price_color",
      "label": "Couleur prix",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "price_weight",
      "min": 400,
      "max": 900,
      "step": 100,
      "label": "Poids prix",
      "default": 700
    },
    {
      "type": "range",
      "id": "original_size",
      "min": 12,
      "max": 20,
      "step": 1,
      "unit": "px",
      "label": "Taille prix original",
      "default": 16
    },
    {
      "type": "color",
      "id": "original_color",
      "label": "Couleur prix original",
      "default": "#9ca3af"
    },
    {
      "type": "range",
      "id": "original_weight",
      "min": 400,
      "max": 700,
      "step": 100,
      "label": "Poids prix original",
      "default": 500
    },
    {
      "type": "range",
      "id": "savings_size",
      "min": 12,
      "max": 20,
      "step": 1,
      "unit": "px",
      "label": "Taille économies",
      "default": 16
    },
    {
      "type": "color",
      "id": "savings_color",
      "label": "Couleur économies",
      "default": "#10b981"
    },
    {
      "type": "range",
      "id": "savings_weight",
      "min": 400,
      "max": 900,
      "step": 100,
      "label": "Poids économies",
      "default": 600
    },
    {
      "type": "header",
      "content": "🔘 Boutons radio"
    },
    {
      "type": "color",
      "id": "radio_border",
      "label": "Couleur bordure",
      "default": "#d1d5db"
    },
    {
      "type": "color",
      "id": "radio_selected_border",
      "label": "Bordure sélectionnée",
      "default": "#3b82f6"
    },
    {
      "type": "color",
      "id": "radio_selected_bg",
      "label": "Arrière-plan sélectionné",
      "default": "#3b82f6"
    },
    {
      "type": "color",
      "id": "radio_dot_color",
      "label": "Couleur du point",
      "default": "#ffffff"
    },
    {
      "type": "header",
      "content": "🛒 Bouton d'ajout au panier"
    },
    {
      "type": "text",
      "id": "btn_text",
      "label": "Texte du bouton",
      "default": "AJOUTER AU PANIER"
    },
    {
      "type": "text",
      "id": "loading_text",
      "label": "Texte de chargement",
      "default": "Ajout en cours..."
    },
    {
      "type": "text",
      "id": "success_text",
      "label": "Texte de succès",
      "default": "Ajouté !"
    },
    {
      "type": "text",
      "id": "error_text",
      "label": "Texte d'erreur",
      "default": "Erreur lors de l'ajout au panier. Veuillez réessayer."
    },
    {
      "type": "color",
      "id": "btn_bg",
      "label": "Arrière-plan",
      "default": "#1a1a1a"
    },
    {
      "type": "color",
      "id": "btn_color",
      "label": "Couleur du texte",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "btn_hover_bg",
      "label": "Arrière-plan au survol",
      "default": "#333333"
    },
    {
      "type": "color",
      "id": "btn_disabled",
      "label": "Arrière-plan désactivé",
      "default": "#9ca3af"
    },
    {
      "type": "text",
      "id": "btn_padding",
      "label": "Espacement",
      "default": "16px 24px"
    },
    {
      "type": "range",
      "id": "btn_radius",
      "min": 0,
      "max": 24,
      "step": 2,
      "unit": "px",
      "label": "Bordure arrondie",
      "default": 8
    },
    {
      "type": "range",
      "id": "btn_size",
      "min": 14,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Taille de police",
      "default": 18
    },
    {
      "type": "range",
      "id": "btn_weight",
      "min": 400,
      "max": 900,
      "step": 100,
      "label": "Poids de police",
      "default": 700
    },
    {
      "type": "select",
      "id": "btn_transform",
      "label": "Transformation",
      "options": [
        {"value": "none", "label": "Aucune"},
        {"value": "uppercase", "label": "Majuscules"},
        {"value": "lowercase", "label": "Minuscules"},
        {"value": "capitalize", "label": "Première lettre"}
      ],
      "default": "uppercase"
    },
    {
      "type": "text",
      "id": "btn_spacing",
      "label": "Espacement des lettres",
      "default": "0.5px"
    },
    {
      "type": "text",
      "id": "btn_hover_transform",
      "label": "Animation au survol",
      "default": "translateY(-2px)"
    },
    {
      "type": "text",
      "id": "btn_hover_shadow",
      "label": "Ombre au survol",
      "default": "0 8px 25px rgba(0, 0, 0, 0.2)"
    },
    {
      "type": "checkbox",
      "id": "redirect_to_cart",
      "label": "Rediriger vers le panier",
      "default": true
    },
    {
      "type": "range",
      "id": "success_delay",
      "min": 500,
      "max": 3000,
      "step": 250,
      "unit": "ms",
      "label": "Délai avant redirection",
      "default": 1500
    },
    {
      "type": "header",
      "content": "💰 Prix total"
    },
    {
      "type": "range",
      "id": "total_size",
      "min": 12,
      "max": 18,
      "step": 1,
      "unit": "px",
      "label": "Taille",
      "default": 14
    },
    {
      "type": "color",
      "id": "total_color",
      "label": "Couleur",
      "default": "#6b7280"
    },
    {
      "type": "range",
      "id": "total_strong_size",
      "min": 14,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Taille (gras)",
      "default": 18
    },
    {
      "type": "color",
      "id": "total_strong_color",
      "label": "Couleur (gras)",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "total_strong_weight",
      "min": 400,
      "max": 900,
      "step": 100,
      "label": "Poids (gras)",
      "default": 700
    },
    {
      "type": "header",
      "content": "🚨 Alerte de stock"
    },
    {
      "type": "checkbox",
      "id": "show_stock_notice",
      "label": "Afficher l'alerte",
      "default": true
    },
    {
      "type": "text",
      "id": "stock_notice_text",
      "label": "Texte",
      "default": "🔥 Stock Limité – VENTE D'ÉTÉ 🔥"
    },
    {
      "type": "text",
      "id": "stock_bg",
      "label": "Arrière-plan",
      "default": "linear-gradient(135deg, #fbbf24, #f59e0b)"
    },
    {
      "type": "color",
      "id": "stock_color",
      "label": "Couleur du texte",
      "default": "#ffffff"
    },
    {
      "type": "text",
      "id": "stock_padding",
      "label": "Espacement",
      "default": "12px 20px"
    },
    {
      "type": "range",
      "id": "stock_radius",
      "min": 0,
      "max": 24,
      "step": 2,
      "unit": "px",
      "label": "Bordure arrondie",
      "default": 8
    },
    {
      "type": "range",
      "id": "stock_size",
      "min": 12,
      "max": 18,
      "step": 1,
      "unit": "px",
      "label": "Taille de police",
      "default": 14
    },
    {
      "type": "range",
      "id": "stock_weight",
      "min": 400,
      "max": 900,
      "step": 100,
      "label": "Poids de police",
      "default": 600
    },
    {
      "type": "header",
      "content": "🛡️ Badges de confiance"
    },
    {
      "type": "checkbox",
      "id": "show_trust_badges",
      "label": "Afficher les badges",
      "default": true
    },
    {
      "type": "text",
      "id": "trust_badge_1",
      "label": "Badge 1",
      "default": "Livraison rapide"
    },
    {
      "type": "text",
      "id": "trust_badge_2",
      "label": "Badge 2",
      "default": "Garantie qualité"
    },
    {
      "type": "text",
      "id": "trust_badge_3",
      "label": "Badge 3",
      "default": "Retour gratuit"
    },
    {
      "type": "range",
      "id": "trust_cols",
      "min": 1,
      "max": 6,
      "step": 1,
      "label": "Nombre de colonnes",
      "default": 3
    },
    {
      "type": "range",
      "id": "trust_gap",
      "min": 8,
      "max": 32,
      "step": 4,
      "unit": "px",
      "label": "Espacement",
      "default": 16
    },
    {
      "type": "color",
      "id": "trust_border",
      "label": "Bordure supérieure",
      "default": "#e5e5e5"
    },
    {
      "type": "text",
      "id": "trust_padding",
      "label": "Espacement des badges",
      "default": "12px"
    },
    {
      "type": "range",
      "id": "trust_radius",
      "min": 0,
      "max": 16,
      "step": 2,
      "unit": "px",
      "label": "Bordure arrondie",
      "default": 8
    },
    {
      "type": "color",
      "id": "trust_hover_bg",
      "label": "Arrière-plan au survol",
      "default": "#f8f9fa"
    },
    {
      "type": "text",
      "id": "trust_hover_transform",
      "label": "Animation au survol",
      "default": "translateY(-2px)"
    },
    {
      "type": "range",
      "id": "trust_icon_size",
      "min": 16,
      "max": 32,
      "step": 2,
      "unit": "px",
      "label": "Taille des icônes",
      "default": 24
    },
    {
      "type": "range",
      "id": "trust_icon_opacity",
      "min": 0.1,
      "max": 1,
      "step": 0.1,
      "label": "Opacité des icônes",
      "default": 0.7
    },
    {
      "type": "color",
      "id": "trust_icon_color",
      "label": "Couleur des icônes",
      "default": "#6b7280"
    },
    {
      "type": "range",
      "id": "trust_text_size",
      "min": 10,
      "max": 16,
      "step": 1,
      "unit": "px",
      "label": "Taille du texte",
      "default": 12
    },
    {
      "type": "color",
      "id": "trust_text_color",
      "label": "Couleur du texte",
      "default": "#6b7280"
    },
    {
      "type": "range",
      "id": "trust_text_weight",
      "min": 400,
      "max": 700,
      "step": 100,
      "label": "Poids du texte",
      "default": 500
    },
    {
      "type": "header",
      "content": "📱 Galerie de médias"
    },
    {
      "type": "range",
      "id": "desktop_media_width",
      "label": "Largeur média desktop",
      "min": 35,
      "max": 65,
      "step": 5,
      "unit": "%",
      "default": 55
    },
    {
      "type": "select",
      "id": "desktop_media_layout",
      "label": "Disposition desktop",
      "options": [
        {"value": "grid_one", "label": "Grille (1x1)"},
        {"value": "grid", "label": "Grille (2x2)"},
        {"value": "grid_highlight", "label": "Grille highlight"},
        {"value": "carousel_thumbnails_left", "label": "Miniatures gauche"},
        {"value": "carousel_thumbnails_bottom", "label": "Miniatures bas"}
      ],
      "default": "carousel_thumbnails_left"
    },
    {
      "type": "select",
      "id": "mobile_media_size",
      "label": "Taille mobile",
      "options": [
        {"value": "expanded", "label": "Étendu"},
        {"value": "contained", "label": "Contenu"}
      ],
      "default": "expanded"
    },
    {
      "type": "select",
      "id": "mobile_carousel_control",
      "label": "Contrôle mobile",
      "options": [
        {"value": "dots", "label": "Points"},
        {"value": "floating_dots", "label": "Points flottants"},
        {"value": "thumbnails", "label": "Miniatures"},
        {"value": "free_scroll", "label": "Défilement libre"}
      ],
      "default": "floating_dots"
    }
  ],
  "presets": [
    {
      "name": "Page Produit Offres"
    }
  ]
}
{% endschema %}